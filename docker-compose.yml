version: '3.8'

services:
  mongo:
    image: mongo:5.0.6
    environment:
      - MONGO_URL=mongodb://mongo:27017/dev
    volumes:
      - mongo_data:/data/db:delegated
    ports:
      - 27017:27017

volumes:
  mongo_data:
